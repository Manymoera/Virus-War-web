<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>Кнопка</title>
    <style>
        .btn {
            display: inline-block; background: #8C959D; color: #fff;
            padding: 1rem 1.5rem; text-decoration: none; border-radius: 3px;
            margin: 0; position: absolute; top: 50%; left: 50%; 
            -ms-transform: translate(-50%, -50%); 
            transform: translate(-50%, -50%);
        }
    </style>
</head>

<body class="page_start">
    <div>
        <% if (!gameStarted) { %>
            <a href="/player1">
                <button class="btn">Начать игру</button>
            </a>
        <% } else { %>
            <a href="/player2">
                <button class="btn">Присоединиться к игре</button>
            </a>
        <% } %>
    </div>
</body>

</html>